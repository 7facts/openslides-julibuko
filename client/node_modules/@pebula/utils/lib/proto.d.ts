import { Constructor } from './mixin';
/**
 * Returns the chain of prototypes up to Object (not included)
 * @pluginApi
 * @param cls
 */
export declare function getProtoChain(cls: Constructor<any>): Array<Constructor<any>>;
export declare function getBaseClass(cls: Constructor<any>): Constructor<any> | void;
